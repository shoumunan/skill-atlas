# Skill Atlas 2.2 · 从处境出发重做「运营」（2026-08-28）

> 起因：作者原话——「运营这一块的功能设计得非常繁琐不易用，甚至我连你到底是干什么的都看不懂，
> 作为一个普通的用户，根本没有从他们的真实需求场景出发去设计。」
> 这条批评成立。本章先认错因，再定新结构。2.1 的方案见 §「2.1 五名词」（保留作对照）。

## 0. 病根：从引擎往外设计，不是从人往里设计

代码里有 `ContextDoctor`，界面上就长出「上下文账单」；有 `MissDetect`，就长出 miss 分类；
有 `Profiles.swift`，就长出「场景包」。收件箱的九类条目，是九个内部数据源的一一映射。

结果是页面名成了架构名：**供给、档位、裁决、回执、命中、回访**——没有一个是人在管理自己电脑
时会用的词。用户看着「供给」两个字，猜不出点进去会发生什么。

三条实测证据（2026-08-28，作者本机）：

| 证据 | 数字 | 说明 |
|---|---|---|
| 场景包实际使用 | 定义 1 个，绑定 0，生效 0，登记项目 0 | 投入几百行代码 + chips + 部分应用态 + 项目绑定，**一次没用过** |
| 收件箱安全 warning | 118 条 | 按「待办队列」设计，但 118 条不是待办。加上限 8 条后，等于从 118 条里随机挑 8 条，永远清不完 |
| 运营三页代码量 | 2041 行 | 换来的是三个用户读不懂的名词 |

## 1. 真正的错：把两种东西塞进同一个队列

- **待办**：agent 请求安装要你批准（0–2 条）、关键级安全命中（12）、装了却用不了（22）。
  有限、必须逐条决定、处理完就没了。
- **属性**：安全提示（118）、触发词重叠（10）、介绍埋太深（2）。
  数量大、不需要逐条决定、是技能自身的性质。

属性该长在技能行上（一个标记 + 一个筛选），不该排队等你「裁决」。我把它们混在一起，于是
队列既清不完、又看不懂。

## 2. 用户真实处境只有五种

1. 想装个新技能
2. 装了但叫不动
3. 听说技能多了吃 token，想瘦一下
4. 不知道哪些还在用
5. 偶尔想把自己的流程做成技能

2、3、4 在用户心里是**同一个瞬间**：「有点不对劲，帮我看看」。我却把它拆成供给页 + 收件箱两页，
各自一套词汇。1 和 5 也是同一个瞬间：「我要多一个技能」。

## 3. 新结构：四项，名字即处境

侧栏不再分组（需要分组本身就说明项太多）：

| 页 | 用户读到时会想 | 里面有什么 |
|---|---|---|
| **技能库** | 我有哪些技能 | 全部技能 + 每行状态标记 + 筛选（含「有问题的」）+ 批量开关 |
| **添加技能** | 我要多一个 | 装现成的（市场 / 链接 / 文件夹 / 收编）+ 自己做一个 |
| **检查** | 有什么要我处理吗 | 只放待办：等你确认 / 可能不安全 / 装了用不了 / 叫不动；外加一张「技能清单占多少 token + 一键清理」 |
| 设置 | 长期配置 | 平台与目录、通知、发现来源、进阶（场景包、使用记录、同步） |

对照消失的东西：

- **供给页** → 解散。token 账单与清理进「检查」；逐技能档位回到技能库（本来就该在技能旁边）；
  场景包与项目绑定降到设置→进阶（0 使用率，不配占一级页）。
- **收件箱** → 更名「检查」，九类砍到四类。触发重叠、介绍不好找、改写回访三类从队列移除：
  它们是诊断指标，不是任务；作为技能行上的标记存在即可。
- **创作** → 并进「添加技能」。「我要多一个技能」只有两条路：装现成的、自己做一个。
  四步向导降级为一个 sheet：起名 → 建好了 → 让会话里的 agent 填内容 → 试一句话看能不能叫到。

## 4. 词汇替换表（黑话 → 人话）

| 旧 | 新 | 理由 |
|---|---|---|
| 供给 | （页面取消） | 用户没有「供给」这个概念 |
| 档位：进清单 / 只手动调 / 不进清单 | **自动 / 点名才用 / 关掉** | 说的是「Claude 会不会自己想到用它」 |
| 收件箱 · 裁决 | 检查 · 处理 | 没人裁决自己的笔记本 |
| 回执 | （直接说结果） | 「账单 8456 → 7100」本身就是回执 |
| miss | 叫不动 | 用户说「这技能怎么叫不动」，不说「发生了一次 miss」 |
| 场景包 | （降级到进阶） | 0 使用率 |

## 5. 不动的部分

引擎一行不改：`Supply.swift` 单写者、`Inbox.swift` 聚合器、装前扫描、CLI 命令面、
zip 通道、多源发现全部保留。这次改的是**这些能力以什么面貌出现在人面前**，不是能力本身。
CLI 是 agent 的 ABI（ADR-12），继续冻结。

## 6. 验收人物换一个

删掉「每周开一次面板的人」——那是我给自己设的、配合运营页存在的人物。换成：

**装了一堆技能、只想让它们好好工作的人**：打开 App，十秒内知道有没有事要处理；
没事就关掉；有事时每件事都能用一句话说清「怎么了、点哪儿」。

---

# Skill Atlas 2.1 · 面板重生：五个名词，一本手册（2026-08-27）

> 本文是战略论证。可执行的工作包见 `PLAN.md`，视觉与交互规范见 `DESIGN.md`（v15 章）。实施 agent 以 PLAN.md 为准。
>
> 参照系：[xingkongliang/skills-manager](https://github.com/xingkongliang/skills-manager)（README.zh-CN）与 [datawhalechina/ai-skills-for-everyone 第 6 课](https://github.com/datawhalechina/ai-skills-for-everyone/blob/main/tutorial/06-manage-many-skills.md)。两份都已通读，教训提炼在 §2。

一句话结论：2.0 解决了「能力长在哪一侧」（agent 侧，方向正确，保留不动），但没解决「面板为什么存在」。v13 把导航塌缩到两页，是把真实能力塞进了设置折叠组和详情页角落——引擎全在、地址全无。2.1 不是再造功能，是给已有能力**发地址**：五个名词、一张入口层级表、一本手册。引擎、CLI 命令面与数据契约都不动，所以这是 minor 版本（2.1.0），不配叫 3.0。

---

## 0. 2.0 之后的实测状态（2026-08-27，全部本机验证）

先说清楚 2.0 做成了什么，避免把已解决的问题再解决一遍：

- **CLI 全量在线**：`atlas 2.0.0`，list/search/info/enable/disable/simulate/doctor/bill/install/review/profile/new/sandbox/slim/paths 全部可用。meta-skill 已挂载、agent 已在真实会话里调用它（本文所在会话即是）。
- **瘦身已生效**：`~/.claude/settings.json` 里 58 条 `skillOverrides`，78 个技能留在 core 档，`atlas bill` 总账 **7,723 token ≤ 8,000 硬指标**。2.0 的 M2 验收实际达成。
- **遥测已开**：`~/.skill-atlas/hooks/log-skill-use.sh` 已装，`usage-events.jsonl` 开始进数据。
- **运维事项真实存在**：`atlas doctor` 报 137 技能、10 个挂载问题、miss 与触发重叠若干——这些正躺在「设置 → 维护」折叠组里等人。

**关键矛盾**：硬指标达标了，作者的体感却是「面板非常用不到实际场景」。税降了 60% 没有被感知，10 个挂载问题没有被看见，瘦身用的是 slim 草案而 profiles 的 bindings 至今为空。结论不是引擎失败，是**面板的可读性失败**：能力没有地址，成果没有回执。一个以「让技能状态可读」为业的工具，自己的状态不可读，这就是最大的产品缺陷。

## 1. 病根：v13 给一个不存在的用户做了减法

v13 的塌缩标准写得很明白：「一个第一次接触 Skill 的人能否……复制调用语、打开软件」。但本产品实际 n=1：用户是 138 个技能、每天几十个 agent 会话的重度用户，「复制调用语」这条 v13 核心桥自 2.0 起已被 agent 直接调用取代。为小白设计的两页导航，把重度用户真正要用的东西全部埋掉：

| 能力（已建成） | 现在的地址 | 结果 |
|---|---|---|
| 供给三档 / 场景包 / 瘦身草案 | 工具栏菜单 + 设置进阶 | profiles bindings 至今为空 |
| 挂载失败 / 安全命中 / miss / 触发重叠 | 设置 → 维护，默认折叠 | 10 个挂载问题无人处理 |
| agent 安装审批 | 深链弹 sheet，无常驻入口 | 错过通知即失联 |
| 沙箱试跑 / atlas new / 处方 A/B | 详情页更多设置深处 | 创作闭环走不完整 |

重复感不是错觉，是可以点名的（2026-08-27 全库审计实测）：

- **9 个不可达的死视图**：`AdoptBanner`、`FilterRow`、`FilterMenu`、`ViewMenu`、`SkillRowView`、`SkillContextMenu`、`CategoryChip`、`PlatformFilterStrip`、`HealthFlag`——仅 LibraryView.swift 里就约 380 行（该文件的 17%）定义了却没有任何引用，全是历次重构换实现后没拆的旧料。
- **11 组活着的重复入口**：安全信息散在 **7** 处（详情横幅、详情安全区、维护两组、安装审阅、待审 sheet、CLI）；瘦身/成本 **5** 处；触发模拟 **4** 处；收编 **3** 处；启停 **5** 处；更新 **5** 处……每处各自半截，没有一处完整。
- **同一状态两个写者**：`SlimPlanner.apply` 与 `ProfileWriter.apply` 各自直写 `skillOverrides`，语义还不一致——供给这个概念在代码里都没归一，界面上自然说不清。
- `Store.swift` 是 2,115 行、约 90 个成员的上帝对象；「项目」在数据模型里不存在（项目级 `.claude/skills` 不扫描，只有 profile apply 能写 settings.local.json）。

**这不是功能太多，是名词太少**——面板只有「我的技能 / 设置」两个词，装不下四类真实工作，于是每类工作都被剁碎塞进缝隙，缝隙之间互相重复。

## 2. 参照系教训：skills-manager 到底清晰在哪

它的功能盘（中央库 + 工作区 + Preset + 同步 + 备份）是红海商品，2.0 的判断不变：不抄功能。要抄的是四条**结构纪律**：

1. **名词与页面 1:1**。它只有七个 view：技能库、安装、全局工作区、项目工作区、备份、设置、概览。每个名词回答一个问题，没有一个名词寄居在另一个名词的折叠组里。
2. **概念正交，三态分离**。它的 meta-skill 开头就教 agent：Library membership（库里有没有）、Preset membership（组里有没有）、Deployment（agent 真看得见吗）是三个独立状态，别混。Skill Atlas 的对应三态（库 / 场景包 / 三档挂载）机制都有，但从未作为概念交给用户。
3. **状态长在卡片上**。每张技能卡为每个 agent 显示一个角标，亮/灭即同步状态，点按即装卸；Preset 标签带 ✓ 与部分安装计数；冲突进「需要处理」队列三选一。状态可见、动作就地——这正是我们的引擎最缺的「回执」。
4. **文档按读者路由**。datawhale 第 6 课先划适用人群（≥5 技能才需要管理器）、给 smoke test（假技能跑通全链路）、写明边界（它不替你判断技能靠不靠谱）。README 的「快速上手」是编号步骤，应用内帮助与 README 同构。**写不成一条用户工作流的功能，就不该存在**——这条纪律比任何页面设计都值钱。

不学的：GUI 为重心（它的 CLI 是后补的，我们相反）；53 个 agent 的广度（本机真实平台就 Claude/Codex 加零星几个）；备份优先（GitSync 维持手动，P2 再议）；1,800 行的设置页。

## 3. 2.1 结构：三 surface 不动，Workbench 从「两页」重排为「五名词」

Agent surface（CLI + meta-skill + 深链）与 Ambient surface（hook + 通知 + 菜单栏）维持 2.0 形态，只做补全。变的是 Workbench：**废除「永不加页」教条**，克制的标准从页面数量改为——**每一页必须能写成手册里的一条工作流；写不成的，砍**。这正是 skills-manager 界面分布的可抄之处：它把「安装」做成一级页面而不是藏在库页的按钮里，每个一级入口只回答一个问题。

侧栏六项、分两组（库：技能库 / 发现；运营：供给 / 收件箱 / 创作；底部：设置）：

| 页面 | 回答的问题 | 吸收（从哪来） | 主要动作 |
|---|---|---|---|
| **技能库** | 我有什么，每个值多少税，状态如何 | 现库页 + 详情运营页转正 | 搜/筛/多选、三态角标点按、详情一屏闭环 |
| **发现** | 我没有的能力，怎么找到并安全装上 | InstallSheet 升格 + 收编 + 多源市场（§9） | 聚合搜索、逛榜单、三合一导入、装前扫描审阅 |
| **供给** | 哪个 AI、哪个项目，带哪些技能进场，花多少 token | ProfileView、SupplyChrome、瘦身草案、项目绑定 | 按范围看账单、场景包 ✓ 一键应用、逐条改档、瘦身草案 |
| **收件箱** | 现在有什么事要我裁决 | 设置→维护全部内容、待审批、可更新、miss | 逐条处理：批准/看 diff/开处方/忽略，清零有回执 |
| **创作** | 怎么把流程沉淀成能触发的技能 | atlas new、孤儿沙箱、处方 A/B、TriggerLab | scaffold → 沙箱试跑 → 触发验证 → 命中回访 |
| 设置 | 长期配置 | 现设置页减去维护 | 平台路径、遥测开关、更新、CLI 软链 |

对应关系检验：装的时候→发现（找与装）+ 收件箱（审批）；用的时候→供给；出问题时→收件箱；创作时→创作。四个时刻各有地址，恰好是 2.0 ROADMAP §1 那张表的补全。**入口深度铁律**：高频动作距启动 ≤2 次点击，折叠组只许藏解释、不许藏动作——逐项对照表见 DESIGN v15 的入口层级表。

**收件箱是差异化所在**：skills-manager 没有安全扫描、没有遥测、没有 miss 检测，所以它不需要收件箱；我们有，所以必须有。v12 那套「行动中心」的交互骨架（主任务卡写清四件事 + 纵向队列 + 完成绿反馈）设计是对的，只是当年填的内容是小白教学；现在填真实运维事件，骨架直接复用。

## 4. 去重清单（重复感的手术台）

- **死代码先清**：§1 点名的 9 个死视图直接删（约 400+ 行），这是零风险的第一刀。
- **skillOverrides 单写者**：`SlimPlanner` 与 `ProfileWriter` 合并为 core 里唯一的供给写入口，谁都不许再直写。
- **复制调用语 / 打开软件**：降为详情页次级动作。主 CTA 换为供给控制与沙箱试跑。
- **安装 / 收编 / 迁移三个入口**：合并为一个「导入」sheet，来源三选（GitHub / 本地目录 / 散落目录收编）。CC Switch 迁移入口收进设置，不再是页面。
- **平台开关三处各说各话**：统一为一个三态模型（完整 / 仅用户可调 / 停用），列表行角标、详情控件、供给页批量操作读写同一模型，视觉语言一致（见 DESIGN v15 的 `SyncDots` / `TierSegment`）。
- **设置 → 维护**：整组解散进收件箱。设置里只留「打开收件箱」一行。
- **BeginnerLoop / 指南残留**：删除。教学职责移交手册（§5）。
- **菜单栏与通知**：一律深链到收件箱对应条目，不再各自弹散装 sheet。

## 5. 手册：把 datawhale 的写法据为己有

新增 `docs/handbook.md`（应用内「帮助」与 README 与之同构），三段式：

1. **你是谁**：路由分层。agent 天天用（你不用读，meta-skill 已教会它）／每周开一次面板的人（读三条工作流）／要把库分享给同事的人（P2 边界）。诚实写明：技能少于 20 个、单平台的人，用 Claude 原生管理就够，不需要本工具。
2. **四条工作流**（每条都是编号步骤 + 完成证据）：① 找并装上一个新技能（发现页搜索或逛榜→装前扫描→审阅→入库回执）；② 给新项目配供给（供给页选范围→套场景包→看账单回执）；③ 每周清一次收件箱（逐条裁决→清零回执）；④ 把流程沉淀成技能（创作页 scaffold→沙箱→触发验证→一周后命中回访）。
3. **边界**：不判断技能内容质量；不做跨机实时同步；不管 MCP/子代理；skillOverrides 只影响 Claude，Codex 靠软链差异。

手册同时是产品法庭：**新功能提案必须先写出它属于哪条工作流的哪一步，写不出就不做。**

## 6. 北极星与验收

指标沿用 2.0 的三个（每会话技能上下文成本 / miss 修复闭环时长 / agent 经由 atlas 的操作数），新增第四个：

4. **收件箱周清零**：每周新检出事项在 7 天内被裁决（含「忽略」）的比例。裁决要留回执（oplog），忽略也算处理。

人物验收更换：删掉「第一次接触 Skill 的小白」（不存在的用户），换成——

- **每周开一次面板的人**：周一打开，两分钟内清完收件箱、看懂本周账单变化，中途不需要进设置。
- **不打开窗口的人**（2.0 保留）：一整周不开主窗口，一切照常。
- **无障碍用户**（保留）。
- **半年后的自己**：忘光了实现细节，凭六项侧栏与手册四条工作流，五分钟内重新接管全库。

## 7. 施工顺序（详见 PLAN.md）

| 期 | 内容 | 说明 |
|---|---|---|
| P0 | 侧栏重排落地（六项两组）：供给页、收件箱页（含维护解散、审批入驻、深链改道） | 结构性重排，价值最大 |
| P1 | 技能库强化（三态角标、用量/成本列、批量操作）+ 详情运营页收尾 + 创作页 | 吸收原 WP5/WP6 未尽部分 |
| P1·并行 | 发现页：导入三合一 + 多源市场（SkillHub / 市场仓库 / 官方精选，见 §9） | WP-M，安装管线护栏不降 |
| P2 | 手册 + 应用内帮助 + README 重写 + meta-skill 文案刷新 | 文档即产品 |
| P3 | 卫生：BeginnerLoop 删除、导入三合一、错版工件清理、截图重拍 | 与 P0–P2 穿插 |

风险与既有护栏（PLAN §7）全部继承：atlas.json 新字段 Optional、只进废纸篓、Theme 令牌纪律、两路构建、hook 永远 exit 0、`~/.claude/skills` 被 mirasim 接管故一律 resolvedRoot()。

## 8. 对「唯一优势是设计」的校正

设计是**可见的**优势；安全扫描、会话级遥测、miss 检测、触发模拟是**独有但不可见**的优势——skills-manager 一样都没有。2.1 的全部工作可以概括为一句话：用前者把后者变成可见的。设计打磨不是独立工作包，而是随页面重排逐页进行（每页一个签名布局、空态设计、状态回执），规范见 DESIGN v15。

## 9. 多源发现：市场负责「找得到」，本地扫描负责「敢不敢装」

现状只接 skills.sh 一个源（Registry.swift，发现型：只填 GitHub 仓库地址进既有安装管线）。用户没有名字或链接时的「帮我找个能 X 的技能」，覆盖面不够，尤其缺国服供给。2026-08-27 实测调研后的接入名单：

| 源 | 性质 | 接法 | 为什么 |
|---|---|---|---|
| skills.sh（已接） | Vercel 全球索引，GitHub 型 | 保持 | 长尾覆盖（GA 后号称 60 万级索引） |
| **SkillHub（skillhub.cn）** | 腾讯自营托管市场：Top 50 策展、企业认证发布方（腾讯文档等官方技能）、版本历史、评测报告 | 公开 API 搜索 + zip 下载通道（实测无鉴权、302 至版本固化 COS 包） | 国服供给唯一像样的来源；中文描述质量高 |
| Claude 原生 marketplace.json 仓库 | 开放格式，任意 git 仓库可做市场 | 用户自加仓库地址，GitHub 管线原样走 | 与官方生态「兼容而非竞争」，零新增网络面 |
| anthropics/skills | Anthropic 官方仓库 | 硬编码精选货架 | 高信任起点，零成本 |
| SEO 型目录站（claudeskillsmarket 等） | 广告站 | 不接 | 无 API、无增量信息、信任不可审计 |

**铁律**：市场只是发现层。评分、下载量、「已审核」徽标、企业认证一律只展示，不减免装前扫描与关键级审批——实测连腾讯官方的 tencent-docs 包里都带 20KB `setup.sh`，这正是扫描存在的理由。多了 zip 型安装物后，审批的内容寻址从 repo@commit 扩展一型 sha256(archive)，语义相同：内容一变，批准失效。方案细节见 PLAN ADR-15/16 与 WP-M。
